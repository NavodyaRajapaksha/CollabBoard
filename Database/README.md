# CollabBoard MongoDB database

Database name: `CollabBoard`

Collections created automatically by Mongoose:
- `users`
- `tasks`

No SQL script is required. Start MongoDB and start the backend; Mongoose creates the collections when the first documents are inserted.
